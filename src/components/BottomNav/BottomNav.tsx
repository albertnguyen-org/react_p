import { BottomNavigation } from '@mui/material';
import React from 'react';

type BottomNavType = {}
const CusBottomNav = (props?: BottomNavType) => {
    return <BottomNavigation></BottomNavigation>
}

export default CusBottomNav;